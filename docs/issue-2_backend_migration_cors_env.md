Issueタイトル: `[Backend](2): DBマイグレーションとCORS・環境変数の初期設定`

### 背景
バックエンドが安全に起動し、フロントエンドと疎通できる状態を先に作る必要がある。特に環境変数の管理、CORS 許可、DB マイグレーションの初期化は、後続の API 実装やフロントエンド連携の前提になるため、最初に固めておくことで手戻りを減らせる。

### 実施内容
- [x] `pydantic-settings` を用いた `.env` 読み込み設定の作成
- [x] FastAPI の `CORSMiddleware` 設定雛形の作成
- [x] Alembic の初期化
- [x] `scores` テーブル作成用の初期マイグレーションファイル作成
- [x] DB 接続設定とマイグレーション実行手順の整備

### 完了条件
- [x] バックエンドが環境変数を読み込んで起動できること
- [x] フロントエンドからのリクエストが CORS で弾かれないこと
- [x] DB に `scores` テーブルが作成されること

### 影響範囲
- バックエンドの設定層
- PostgreSQL の初期スキーマ
- フロントエンドとの API 通信
- 開発環境の起動手順

### 開発フェーズ
- マイルストーン0「基盤構築」
- Phase 1: 開発基盤と初期設定の完了

### 参考資料
- [docs/DEVELOPMENT_PLAN.md](docs/DEVELOPMENT_PLAN.md)
- [docs/issue-0_infra.md](docs/issue-0_infra.md)

### 見積もり工数
- `.env` 読み込み設定: 1.0h
- CORS 設定雛形作成: 0.5h
- Alembic 初期化: 1.0h
- `scores` テーブル用マイグレーション作成: 1.5h
- 動作確認と調整: 0.5h
- 合計目安: 4.5h
