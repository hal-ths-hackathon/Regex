# =============================================================================
# frontend/.env.example — フロントエンド環境変数テンプレート
# =============================================================================
# このファイルをコピーして .env を作成し、ローカル開発環境の設定を行ってください。
#   cp .env.example .env
# =============================================================================

# バックエンドの API ベースURL（フロントエンドからリクエストを送信するAPIのエンドポイント）
VITE_API_BASE_URL=http://localhost:8090
